# Description: Web Dashboard (with REST APIs) for EasyTack platform
# Author: Kobiljon Toshnazarov
# Last updated: March 8, 2023

version: "3.8"

services:
  dashboard:
    container_name: easytrack-dashboard

    build:
      context: .
      dockerfile: Dockerfile

    environment:
      # Django development server settings
      DEBUG: ${DEBUG}
      SERVERNAMES: ${SERVERNAMES}
      INTERNAL_IPS: ${INTERNAL_IPS}

      # Database credentials
      POSTGRES_HOST: easytrack-postgres
      POSTGRES_PORT: ${POSTGRES_PORT}
      POSTGRES_DBNAME: ${POSTGRES_DBNAME}
      POSTGRES_TEST_DBNAME: ${POSTGRES_TEST_DBNAME}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

      # Google API credentials
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}

      # Django social auth credentials
      SOCIAL_AUTH_GOOGLE_OAUTH2_KEY: ${SOCIAL_AUTH_GOOGLE_OAUTH2_KEY}
      SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET: ${SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET}

      # Django secret key
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}

      # Test account details (app user)
      TEST_ACCOUNT_EMAIL: ${TEST_ACCOUNT_EMAIL}
      TEST_ACCOUNT_NAME: ${TEST_ACCOUNT_NAME}

    ports:
      - "8000:8000"

    networks:
      - "easytrack-network"

networks:
  easytrack-network:
    external: true
